<!DOCTYPE html>
<html>
<head>
  <title>ToDo編集</title>
  <link rel="stylesheet" href="/style.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <body class="container mt-4">
  <h1 class="mb-4">ToDo編集</h1>
  <form action="/update/<%= todo.id %>" method="post" class="mb-3">
    <div class="mb-3">
      <label class="form-label">タイトル:
        <input type="text" name="title" value="<%= todo.title %>" class="form-control" required>
      </label>
    </div>
    <div class="mb-3">
      <label class="form-label">内容:
        <input type="text" name="content" value="<%= todo.content %>" class="form-control">
      </label>
    </div>
    <div class="mb-3">
      <label class="form-label">期限:
        <input type="date" name="due_date" value="<%= todo.due_date %>" class="form-control">
      </label>
    </div>
    <div class="mb-3">
      <label class="form-label">優先度:
        <input type="number" name="priority" min="1" max="5" value="<%= todo.priority %>" class="form-control">
      </label>
    </div>
    <button type="submit" class="btn btn-primary">更新</button>
  </form>
  <a href="/" class="btn btn-secondary">戻る</a>
  </body>
</body>
</html>
